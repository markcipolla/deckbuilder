#= require backbone

# this is a model for pages stored in our database (Home, Business, etc). If
# you're looking for the view that initialises each page, checkout the
# BasePage class.
class Deck.Page extends Backbone.Model
